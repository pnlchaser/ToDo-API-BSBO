# ToDo API с матрицей Эйзенхауэра (v2.0)

## Описание
RESTful API для управления задачами с классификацией по матрице Эйзенхауэра (Q1-Q4) и функциональностью дедлайнов. Приложение автоматически определяет срочность задач на основе оставшихся дней до дедлайна и помогает расставить приоритеты.

## Основные возможности
- ✅ **CRUD операции** над задачами (создание, чтение, обновление, удаление)
- 📅 **Дедлайны**: каждая задача имеет плановый срок выполнения
- ⚡ **Автоматическое определение срочности**: задача считается срочной, если до дедлайна остается ≤3 дней
- 📊 **Матрица Эйзенхауэра**: автоматическая классификация задач по квадрантам (Q1-Q4)
  - **Q1**: важные и срочные
  - **Q2**: важные, но не срочные
  - **Q3**: не важные, но срочные
  - **Q4**: не важные и не срочные
- 🔍 **Поиск задач** по названию и описанию
- 📈 **Статистика**: общая информация о задачах и статистика по дедлайнам
- 💾 **Расчетные поля**: автоматический расчет дней до дедлайна в ответах API

## Технологии
- **Python** 3.13
- **FastAPI** - веб-фреймворк
- **SQLAlchemy** - ORM
- **asyncpg** - асинхронный драйвер PostgreSQL
- **Pydantic** v2 - валидация данных
- **Uvicorn** - ASGI сервер
- **PostgreSQL** (Supabase) - база данных

## Установка и запуск

### Предварительные требования
- Python 3.11+
- PostgreSQL база данных или Supabase аккаунт
- pip

### Пошаговая установка

```bash
# 1. Клонируйте репозиторий
git clone <repo-url>
cd ToDo-API-BSBO

# 2. Создайте и активируйте виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Создайте файл .env с конфигурацией
cat > .env << EOF
DATABASE_URL="postgresql+asyncpg://user:password@host:port/dbname"
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=development
EOF

# 5. (Первый запуск) Примените миграцию для добавления столбца deadline_at
python migrate_add_deadline.py

# 6. Запустите сервер
uvicorn main:app --reload
```

Сервер будет доступен по адресу: `http://127.0.0.1:8000`

## API Эндпоинты

### Управление задачами

#### Создать новую задачу
```http
POST /api/v2/tasks/
Content-Type: application/json

{
  "title": "Подготовить отчет",
  "description": "Квартальный отчет для руководства",
  "is_important": true,
  "deadline_at": "2025-12-20T17:00:00Z"
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "title": "Подготовить отчет",
  "description": "Квартальный отчет для руководства",
  "is_important": true,
  "deadline_at": "2025-12-20T17:00:00Z",
  "quadrant": "Q1",
  "completed": false,
  "created_at": "2025-12-14T10:30:00Z",
  "days_to_deadline": 6
}
```

#### Получить все задачи
```http
GET /api/v2/tasks/
```

#### Получить задачу по ID
```http
GET /api/v2/tasks/{task_id}
```

#### Обновить задачу
```http
PUT /api/v2/tasks/{task_id}
Content-Type: application/json

{
  "title": "Новое название",
  "deadline_at": "2025-12-25T17:00:00Z",
  "is_important": false
}
```

#### Отметить задачу выполненной
```http
PATCH /api/v2/tasks/{task_id}/complete
```

#### Удалить задачу
```http
DELETE /api/v2/tasks/{task_id}
```

### Поиск и фильтрация

#### Поиск по названию/описанию
```http
GET /api/v2/tasks/search?q=отчет
```

#### Задачи по статусу
```http
GET /api/v2/tasks/status/{status}
```
- `status`: `pending` (не выполнены) или `completed` (выполнены)

#### Задачи по квадранту
```http
GET /api/v2/tasks/quadrant/{quadrant}
```
- `quadrant`: `Q1`, `Q2`, `Q3` или `Q4`

### Статистика

#### Общая статистика
```http
GET /api/v2/stats/
```

**Ответ:**
```json
{
  "total_tasks": 15,
  "by_quadrant": {
    "Q1": 3,
    "Q2": 5,
    "Q3": 4,
    "Q4": 3
  },
  "by_status": {
    "completed": 7,
    "pending": 8
  }
}
```

#### Статистика по дедлайнам (невыполненные задачи)
```http
GET /api/v2/stats/deadlines
```

**Ответ:**
```json
[
  {
    "id": 1,
    "title": "Срочный отчет",
    "description": "Необходимо доставить сегодня",
    "created_at": "2025-12-14",
    "deadline_at": "2025-12-14T17:00:00Z",
    "days_until_deadline": 0,
    "quadrant": "Q1",
    "is_important": true
  },
  {
    "id": 2,
    "title": "Планировка на неделю",
    "description": "Подумать о приоритетах",
    "created_at": "2025-12-12",
    "deadline_at": "2025-12-25T17:00:00Z",
    "days_until_deadline": 11,
    "quadrant": "Q2",
    "is_important": true
  }
]
```

## Структура проекта

```
ToDo-API-BSBO/
├── main.py                    # Точка входа приложения
├── database.py                # Конфигурация БД и подключение
├── schemas.py                 # Pydantic схемы (валидация)
├── models/
│   └── task.py               # SQLAlchemy модель Task
├── routers/
│   ├── tasks.py              # Эндпоинты для управления задачами
│   └── stats.py              # Эндпоинты статистики
├── requirements.txt           # Зависимости проекта
├── .env                       # Переменные окружения
├── migrate_add_deadline.py    # Скрипт миграции БД
└── README.md                  # Документация
```

## Логика определения срочности и квадранта

### Срочность
Задача считается **срочной** (`is_urgent: true`), если до её дедлайна остается **3 дня или менее**.

```python
days_left = (deadline_date - today).days
is_urgent = days_left <= 3
```

### Определение квадранта
Квадрант определяется на основе двух параметров: **важность** (`is_important`) и **срочность** (`is_urgent`, вычисляется из дедлайна):

| Важность | Срочность | Квадрант | Описание |
|----------|-----------|----------|---------|
| ✅ Да    | ✅ Да     | **Q1**   | Кризисное управление; требует немедленного внимания |
| ✅ Да    | ❌ Нет    | **Q2**   | Стратегическое развитие; планирование и развитие |
| ❌ Нет   | ✅ Да     | **Q3**   | Дела других людей; много отвлечений |
| ❌ Нет   | ❌ Нет    | **Q4**   | Пустая трата времени; всё, что можно исключить |

## Примеры использования

### Пример 1: Создание срочной задачи (Q1)
```bash
curl -X POST http://localhost:8000/api/v2/tasks/ \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Исправить ошибку в production",
    "description": "Критическая ошибка в платежной системе",
    "is_important": true,
    "deadline_at": "2025-12-14T18:00:00Z"
  }'
```

### Пример 2: Получить все невыполненные Q1 задачи
```bash
# Сначала получаем статистику по дедлайнам
curl http://localhost:8000/api/v2/stats/deadlines | jq '.[] | select(.quadrant == "Q1")'
```

### Пример 3: Поиск задач
```bash
curl "http://localhost:8000/api/v2/tasks/search?q=отчет"
```

### Пример 4: Обновление дедлайна
```bash
curl -X PUT http://localhost:8000/api/v2/tasks/1 \
  -H "Content-Type: application/json" \
  -d '{
    "deadline_at": "2025-12-25T17:00:00Z"
  }'
```

## Интерактивная документация

После запуска сервера используйте встроенную интерактивную документацию:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## Миграции БД

При первом запуске на новой базе данных все таблицы создаются автоматически через `init_db()` в `main.py`.

Если вы добавляете новое поле в существующую БД:

```bash
python migrate_add_deadline.py
```

Этот скрипт безопасно добавляет столбец `deadline_at` без потери данных (устанавливает дефолтное значение на дату в будущем).

## Переменные окружения

```env
# PostgreSQL подключение (Supabase или локальный сервер)
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database

# Сервер
API_HOST=0.0.0.0           # Адрес прослушивания
API_PORT=8000              # Порт
ENVIRONMENT=development    # development или production
```

## Обработка ошибок

API возвращает стандартные HTTP коды статуса:
- **200 OK** - успешный запрос
- **201 Created** - ресурс успешно создан
- **400 Bad Request** - некорректный запрос (неверная схема данных)
- **404 Not Found** - ресурс не найден
- **500 Internal Server Error** - ошибка сервера

Ошибки возвращаются в формате:
```json
{
  "detail": "Описание ошибки"
}
```

## Развертывание

### На Heroku
```bash
git push heroku main
```

### На собственном сервере
```bash
# Установите systemd сервис или используйте supervisor для управления процессом
# Используйте nginx как reverse proxy перед uvicorn
```

## Тестирование

```bash
# Проверка здоровья API
curl http://localhost:8000/health

# Получить основную информацию
curl http://localhost:8000/
```

## Автор
Чистяков Сергей

## Лицензия
MIT License

## История версий

### v2.0.0 (текущая)
- ✨ **Новое**: функциональность дедлайнов (`deadline_at`)
- ✨ **Новое**: автоматическое определение срочности на основе дедлайна
- ✨ **Новое**: расчетное поле `days_to_deadline` в ответах
- ✨ **Новое**: эндпоинт `/stats/deadlines` для статистики по срокам
- 🔄 **Изменено**: логика определения квадранта теперь использует дедлайны вместо флага `is_urgent`
- 🔄 **Изменено**: параметр `is_urgent` теперь вычисляется автоматически при создании/обновлении

### v1.0.0
- Базовый CRUD для задач
- Матрица Эйзенхауэра (Q1-Q4)
- Статистика по квадрантам
